# Salad Cat — M9P Art Pack Requirements

- `BASE_FIGHTER`: existing 250-frame Salad Cat body pack.
- `WORLD_EFFECT`: `reject_shockwave_l1`, `reject_shockwave_l2`, `reject_shockwave_l3`.
- `ULTIMATE_SCREEN`: `salad_ultimate_background` (16:9; runtime 1280×720).
- `HAZARD`: `salad_bowl_high`, `salad_ground_impact_low`, `target_marker`.

The body `special_neutral` animation shows only Salad Cat's charge/release/recoil. Expanding shockwave lifetime is detached art. The Ultimate body animation is activation/casting/handoff/recovery; the bowl remains a world-space hazard because its location is gameplay-driven.
