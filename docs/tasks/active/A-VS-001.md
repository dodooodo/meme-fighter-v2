---
id: A-VS-001
stage: A
type: audit
status: ready
dependencies: []
allowed_paths: [docs/stages/, docs/tasks/, tests/characters/, tests/roster/, tests/replay/, tests/snapshot/, VALIDATION_REPORT.txt]
forbidden_paths: [battle/, fighter/, data/, presentation/, frontend/, assets/]
required_specs: [AGENTS.md, ARCHITECTURE.md, docs/roadmap/PRODUCTION_ROADMAP.md]
required_checks: [python3 scripts/static_validate.py, godot --headless --path . -s res://tests/run_tests.gd]
---
# Task
## Goal
Audit `magic_orange_cat` and `salad_cat` against Stage A Golden Pair gameplay coverage.
## Context
They are roster data at `data/characters/magic_orange_cat.tres` and `data/characters/salad_cat.tres`; roster tests exist under `tests/characters/roster/`.
## Existing Behavior To Preserve
This is evidence collection: no combat/data/presentation behavior changes.
## Required Change
Create a coverage matrix for walk, crouch, jump, dash/backstep, guard, normals, air attack, throw, special, ultimate, reactions, knockdown/getup, and KO; map each item to test/runtime/manual evidence.
## Public/API Contract
None.
## Implementation Constraints
Separate source/static evidence from actual runtime/manual evidence.
## Edge Cases
Mark missing/unexecuted coverage explicitly; do not infer production visual coverage.
## Tests
Run existing static/runtime suite where available.
## Acceptance Criteria
Both character IDs have a traceable, truthful matrix and follow-up gaps become discrete tasks.
## Rollback / Recovery Notes
Remove the audit artifact only if replaced by a newer evidence-based audit.
## Out of Scope
Game-feel, art, or gameplay fixes.
