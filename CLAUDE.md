@AGENTS.md

# Claude Code compatibility

Use `AGENTS.md` and the linked canonical documentation as the source of truth.
For architecture-changing work, write a concise plan before editing. Prefer the
repo-local workflow skills in `.agents/skills/`. Before claiming completion, run
the task checks, project verification, and a scope/diff audit. Do not expand a
task packet's scope or create Claude-only architecture rules.
